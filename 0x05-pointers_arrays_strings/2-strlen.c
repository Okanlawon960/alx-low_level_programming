#include "main.h"
#include <string.h>

/**
 * _strlen - Kindly refer to the dir README file
 *
 * @s: The string paramenter pass to the function
 *
 * Return: Return the length of the string
 */
int _strlen(char *s)
{
	size_t length = strlen(s);

	return (length);
}
